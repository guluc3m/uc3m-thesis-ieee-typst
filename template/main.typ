#import "/lib.typ": conf, gls, glspl
// #import "glossary.typ": glossary-entries as glossary // uncomment this line and the config if you want a glossary

#show: conf.with(
  degree: "Ingeniería Typstática",
  title: "Análisis, diseño, e implementación del mejor Trabajo de Fin de Grado de la historia",
  short-title: "La mejor Memoria de la Historia",
  author: "Nombre Extremadamente Largo e Incómodo de Escribir",
  advisors: ("Profesor Cuyos Padres Tenían Visión de Futuro",),
  location: "Leganés, Madrid",
  thesis-type: "TFG",
  date: datetime(year: 2025, month: 4, day: 20),
  language: "es",
  style: "fancy",
  // titlepage-style: "fancy",
  license: true,
  double-sided: true,
  flyleaf: true,
  bibliography-file: "/template/references.bib",
  epigraph: (
    quote: [Cacaaaaaaaaaaaa.],
    author: "Uno que se cagaba",
    // source: "",
  ),
  abstract: (
    body: [Ta wapo.],
    keywords: ("Caca", "Culo", "Pedo", "Pis"), // see https://www.ieee.org/content/dam/ieee-org/ieee/web/org/pubs/ieee-taxonomy.pdf
  ),
  english-abstract: (
    body: ['tis cool.],
    keywords: ("Poop", "Butt", "Fart", "Pee"),
  ),
  acknowledgements: [Mi churri.],
  outlines: (
    // contents is compulsory
    figures: true,
    tables: true,
    listings: false,
    // custom: []
  ),
  // appendixes: [],
  // glossary: glossary,
  // abbreviations: (TFG: "Trabajo de Fin de Grado"),
  genai-declaration: (
    usage: true,
    data-usage: (
      sensitive: "with-authorization",
      copyright: "without-authorization",
      personal: "not-used",
      followed-terms: true,
      explanation: [_[Add an example] e.g. "the materials were protected but
        permitted use for this purpose" or "the terms of use, which can be found
        at this address (...), prevent the use I have made, but it was essential
        given the nature of the work_],
    ),
    technical-usage: (
      // If you think it is necessary to include it, add it in each case:
      // *using the prompt* ...
      //            (write the request made to the IAG)
      // *having as interaction* ...
      //            (describe what interaction you made with the IAG after the
      //            prompt's response)
      //
      // Comment out the ones that don't apply.
      //
      // Example: I declare that I have made use of the Generative AI system
      // Chat GPT 3.5 to search for information using the prompt: "Tell me a
      // concrete example that illustrates the application of the urban planning
      // proposal of the 15-minute City in Spain", having as interaction the
      // proposition of the concrete example of the Chamartín district that has
      // been reflected in the report.
      documentation: [_[Add an example] For example: I have asked for a list of
        alternatives to address the problem stated at the beginning of the
        paper._],
      review: [_[Add an example] For example: I have asked for paragraphs to be
        rewritten to shorten the conclusions while keeping the same ideas
        already written._],
      research: [_[Add an example] For example: The questions asked were aimed
        at [finding definitions, explanations of processes, examples of use,
        lists of risks of use...]._],
      references: [_Bibliography search._],
      summary: [_Summary of bibliography consulted._],
      translation: [_Translation of texts consulted._],
      assistance-coding: [_[Add an example of the usual flow of use for
        programming] For example: I have asked for explanations about a code in
        Python._],
      generating-content: [_[indicate its use in this section, as well as in a
        footnote to the figure/element generated by Generative AI] For example:
        Source: own elaboration with the help of Midjourney._],
      optimization: [_[Add an example of the usual flow of use to optimise] For
        example: I have asked for help to correct and explain errors in a code
        that was not working properly._],
      data-processing: [_[Add an example] For example: after collecting the
        data, I have asked for guidance in normalising the data in preparation
        for the subsequent analysis._],
      idea-inspiration: [_[Add an example] For example: once I was clear about
        the objective of the work, I asked for ideas to cover the project from a
        multidisciplinary approach._],
      other: [_Other uses linked to the generation of specific points of the
        specific development of the work._],
    ),
    usage-reflection: [_Please provide a personal assessment (free format) of
      the strengths and weaknesses you have identified in the use of Generative
      AI tools in the development of your work. Mention if it has helped you in
      the learning process, or in the development or drawing conclusions from
      your work._],
  ),
)



= Introduction
#lorem(50)

== In the beninging
#lorem(400)

=== Some cool stuff
#lorem(50)

==== But not _that_ cool
#lorem(40)


== Motivation
#lorem(140)

== Problem Statement
#lorem(50)

= Related Work
#lorem(200)

#figure(
  image("/img/old_uc3m_logo.svg", width: 70%),
  caption: [El mejor logo de la UC3M, con diferencia],
) <logo>


= Tables

#let check = [$checkmark$]
#let uncheck = []
#figure(
  caption: "Feature comparison of current assembly simulators",
  {
    set text(9.4pt)
    block(
      stroke: (y: 0.7pt),
      table(
        columns: (34%, auto, auto, auto, auto, auto),
        row-gutter: -0.2em,
        stroke: (_, y) => if y == 0 { (bottom: 0.2pt) },
        table.header(
          [*Simulator*], [Kite], [ARMLite], [Sail], [CREATOR], [Proposal]
        ),
        align: (left, horizon, horizon, horizon, horizon, horizon),

        [*Language*], [C/C++], [JavaScript], [OCaml], [JavaScript], [ISO C++20],

        [*License*],
        [BSD-3-Clause],
        [None],
        [BSD-2-Clause],
        [LGPLv2.1],
        [GPLv3],

        [*CLI*], check, uncheck, uncheck, check, check,
        [*I/O*], uncheck, check, uncheck, check, check,
        [*Step-by-step execution*], uncheck, uncheck, uncheck, check, check,
        [*Simple architecture definition*],
        uncheck,
        uncheck,
        uncheck,
        check,
        check,

        [*Native execution*], check, uncheck, check, uncheck, check,
        [*In-browser*], uncheck, check, uncheck, check, [$checkmark^*$],
        [*Error checking*], uncheck, check, uncheck, check, check,
        [*Architecture validation support*],
        uncheck,
        uncheck,
        uncheck,
        uncheck,
        check,
      ),
    )
    set align(left)
    [#super("*") Future work]
  },
)

== Graph Example
#include "graph_example.typ"


// Referencing an acronym or the glossary is very important, ie. if you don't know
// what an #gls("API") is, you should look it up.
